'use strict';
const path = require('path')

module.exports = function login(token) {
if (token == "" || token == undefined) throw new TypeError("Please Provide A Token.");
module.exports = { token }
return (token)
};
